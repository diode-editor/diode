/**
 * ВРЕМЕННЫЙ файл. Проверка того, что мутационный гейт на PR действительно
 * краснеет: тест рядом покрывает все ветки, но ничего не проверяет, поэтому
 * мутанты обязаны выжить. PR с этим файлом мержить нельзя — он закрывается
 * сразу после того, как CI покажет красное.
 */

/** Ограничивает значение отрезком [min, max]. */
export function clamp(value: number, min: number, max: number): number {
    if (value < min) return min;
    if (value > max) return max;
    return value;
}
