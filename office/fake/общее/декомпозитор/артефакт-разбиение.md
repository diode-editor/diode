---
kind: разбиение
audience: ['*']
nodes:
  - описание: Дерево файлов показывает git-статус файла цветом и буквой, как в vscode
  - описание: Статус-бар показывает текущую ветку и число изменённых файлов
---
Первая подзадача ставит источник статуса, вторая его читает — в этом порядке.
