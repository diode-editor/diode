{
    "name": "inline-ghost",
    "displayName": "Inline Ghost",
    "description": "E2E demo extension: canned inline completions (ghost text) with an artificial LLM-like delay.",
    "version": "0.0.1",
    "publisher": "test",
    "main": "./extension.js",
    "engines": {
        "vscode": "^1.0.0"
    },
    "contributes": {
        "configuration": {
            "title": "Inline Ghost",
            "properties": {
                "inlineGhost.responseDelay": {
                    "type": "number",
                    "default": 250,
                    "description": "Milliseconds the demo provider waits before answering. Raise it above editor.inlineSuggest.requestTimeout to watch the editor give up on a slow provider."
                }
            }
        }
    }
}
