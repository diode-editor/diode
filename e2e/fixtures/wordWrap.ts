// word-wrap fixture: длинные строки для проверки переноса по словам.
// Первая длинная строка кончается маркером, которого не видно без переноса.
export const longProse = "The editor keeps every character of this very long sentence on a single logical line, so without word wrap the tail can only be reached by horizontal scrolling — WRAP_TAIL_MARKER";
export const short = "short line between the long ones";
export const longCode = ["alpha", "beta", "gamma", "delta", "epsilon", "zeta", "eta", "theta", "iota", "kappa", "lambda"].join(" + ");
