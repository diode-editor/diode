# Система контекста (when)

## Цель

Реализовать систему контекстных условий по аналогии с VS Code `when` clause — определяет, когда команда/кейбиндинг/пункт меню активен.

`ContextKeyService` и интеграция с `KeybindingRegistry`/меню уже сделаны (описание — [arch/Workbench.md](../arch/Workbench.md), раздел про систему команд). Осталось:

## Осталось

### [ ] Парсинг и вычисление when-выражений
Сейчас используется `new Function(...)` с подстановкой переменных. Заменить на полноценный парсер when-выражений (`&&`, `||`, `!`, сравнения) для безопасности и расширяемости.

Что уже пришлось подпереть внутри этого подхода (и что парсер должен снять):
- **Точечные ключи расширений** (`supermaven.isProUser`, их приносит команда `setContext`)
  параметром `new Function` быть не могут — `buildScope` отдаёт вместо них вложенные
  объекты под корневым сегментом. Побочные ограничения: ключ, чей корень столкнулся с
  плоским ключом, вычислителю не виден, и негодные в идентификатор имена (`foo-bar`,
  `2fa`, `class`) отбрасываются. Настоящий парсер про идентификаторы JS не знает и
  может брать имя ключа целиком, каким бы оно ни было.
- **Оператор `in`** (VS Code умеет `key in arrayKey`) не поддержан: значения у нас
  примитивные, массив из `setContext` сводится к своей истинности.
