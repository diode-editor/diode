# Матрица готовности: API расширений VS Code

> **Заготовка.** Структура и легенда готовы; строки таблицы — примеры, которые будут заменены
> сгенерированными данными. Источник правды — запиннённый `vscode.d.ts`: активная поверхность API —
> это дословно раскомментированные строки его нижней копии, поэтому матрица собирается скриптом из
> исходников и не может ни приукрасить, ни устареть. Скрипт генерации — TODO.

**Конечная цель diode — полная поддержка API расширений VS Code везде, где она имеет смысл в
терминале.** Всё, что по природе требует браузера (webview и UI-heavy поверхности), не будет
поддержано никогда — и это указано в матрице явно, а не замолчано. Важная асимметрия: языковая
поддержка экосистемы (LSP, грамматики, темы, сниппеты, форматтеры) через webview не ходит — она
живёт в обычном extension API, который здесь есть.

Версия API: см. `extensions/VSCODE_VERSION` (матрица генерится против этого пина).

## Легенда

| статус | значение |
| --- | --- |
| ✅ | поддержано — раскомментировано в `vscode.d.ts`, закрыто тестами |
| 🟡 | частично — поверхность есть, отмеченные члены пока стабы |
| 🕐 | запланировано — в роадмапе, пока закомментировано |
| ⛔ | не будет by design — требует браузера/GUI |

## Матрица

_Строки ниже — примеры формата; заменить генерацией из `vscode.d.ts`._

| поверхность | статус | комментарий |
| --- | :-: | --- |
| `vscode.languages` (definition, hover, references, completion, diagnostics, code actions, formatting, …) | 🟡 | пример |
| `vscode.commands` | 🟡 | пример |
| `vscode.window` (редакторы, сообщения, quick pick, статус-бар) | 🟡 | пример |
| `vscode.window.createWebviewPanel` | ⛔ | webview — требует браузера |
| `vscode.workspace` (файлы, конфигурация, события) | 🟡 | пример |
| `vscode.workspace.applyEdit` | ✅ | пример |
| `vscode.scm` | 🕐 | пример |
| `vscode.debug` | 🕐 | пример |
| `vscode.env` | 🟡 | пример |

## Как читать «частично»

«Частично» — не «работает вполсилы», а «поверхность объявлена, перечисленные члены — стабы».
Генератор должен выводить в комментарий, какие именно члены не активны, чтобы автор расширения мог
проверить свой случай за минуту, не запуская diode.
